import UIKit

class ShowBulletinViewController: UIViewController {
	private let bulletin: BulletinResource

	required init(coder aDecoder: NSCoder) {
		print("Loading from NIB not supported.")
		abort()
	}
}

